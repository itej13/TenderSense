# Copy to .env.local and fill in values from Google Cloud Console.
# Never commit .env.local.

# OAuth 2.0 Web client ID (ends with .apps.googleusercontent.com)
VITE_GOOGLE_CLIENT_ID=

# Comma-separated Google emails that get the admin role.
# Everyone else who signs in becomes an employee.
VITE_ADMIN_EMAILS=
